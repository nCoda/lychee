machine:
  python:
    version: 2.7.11

dependencies:
  override:
    - pip install -r ci_requirements.txt

test:
  override:
    - py.test --cov=lychee --junit-xml=$CIRCLE_TEST_REPORTS/pytest/junit.xml
